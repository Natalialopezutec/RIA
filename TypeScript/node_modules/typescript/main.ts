import { User } from '../../user.model';
let numero: number =10;
let texto: String = "Texto en una variable String";
let booleano: Boolean = true;
let cualquierTipo: any; 


let usr1: User;

//Array

let frutas: String[] = [ 
    "Manzana", "Pera", "Banana"
];

let tupla: [String, number];

//tupla = ["prcio"];

enum color{
    Rojo, Verde, Azul
}

let c: color = color.Azul;

//Enum
enum combustible {
    Nafta = 3,
    Gasoil =7
}

let com: combustible = combustible.Gasoil;
console.log(com); //imprime 7

//Poco usando
let vacio: void;
let indefinido: undefined;
let nulo: null=null;

//tipado de retorno de funciones:
function suma(a:number, b:number): number | void{
    return a +b;
}